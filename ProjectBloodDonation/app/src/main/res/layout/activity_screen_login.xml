<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/img_login"
    android:padding="10dp"
    tools:context=".ScreenLoginActivity">

    <LinearLayout
        android:id="@+id/head"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="@font/baloo"
            android:paddingTop="25dp"
            android:text="BLOOD DONATION - HIẾN MÁU VÌ CỘNG ĐỒNG"
            android:textColor="#EF0707"
            android:textSize="18dp"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/tenDangNhap"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Tên đăng nhập / Email"
            android:inputType="textEmailAddress"
            android:paddingTop="50dp"
            android:textColor="@color/black"
            android:textSize="18dp" />

        <EditText
            android:id="@+id/matKhau"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="25dp"
            android:hint="Mật khẩu"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/btnLogin"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="15dp"
            android:drawableLeft="@android:drawable/ic_lock_lock"
            android:gravity="center"
            android:onClick="login"
            android:text="Đăng nhập"
            android:textSize="18dp"
            android:textStyle="bold"
            app:backgroundTint="#F8460E" />

        <TextView
            android:id="@+id/txtDangKi"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:gravity="center"
            android:onClick="moveRegister"
            android:text="Đăng kí"
            android:textColor="@color/black"
            android:textSize="18dp"
            android:textStyle="bold" />

    </LinearLayout>
    <ProgressBar
        android:id="@+id/formPrg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_below="@+id/head"
        android:outlineSpotShadowColor="#EF0707"
        android:visibility="gone" />


</RelativeLayout>
